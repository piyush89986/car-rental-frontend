# Production: your deployed API (must use https when the site is on https)
# Must end with /api (or it will be added automatically)
REACT_APP_API_URL=https://your-api.onrender.com/api

# Optional: API origin for uploaded images (defaults to REACT_APP_API_URL without /api)
REACT_APP_SERVER_URL=https://your-api.example.com
